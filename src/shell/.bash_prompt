#!/usr/bin/env bash

# enable thefuck
if which thefuck > /dev/null; then
    eval "$(thefuck --alias)"
fi

# automatically trim long paths in prompt
# requires bash 4.x
PROMPT_DIRTRIM=2

if [[ -n "$ZSH_VERSION" ]]; then # quit if in zsh
    return 1 2> /dev/null || exit 1
fi

